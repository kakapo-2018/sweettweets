export function saveTweet(tweet) {
  console.log("hello im an action");
  console.log("you entered " + tweet);

  // return function(dispatch) {
  //   request("get", "/bags").then(response => {
  //     if (!response.ok) {
  //     } else {
  //       dispatch(receiveAddBag(null, response.body.bag));
  //     }
  //   });
  // };
}
